export interface BundleOffer {
  id: string;
  name: string;
  productId: string;
  productTitle: string;
  code: string;
  compareAtPrice: string;
  discountedPrice: string;
  enabled: boolean;
  discountId?: string;
  createdAt: string;
  updatedAt: string;
}
