query CartTransformInput {
  cart {
    lines {
      id
      quantity
      merchandise {
        ... on ProductVariant {
          id
        }
      }
    }
  }
  shop {
    metafield(namespace: "$app:gwp", key: "gift_config") {
      value
    }
  }
}
